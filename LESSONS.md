- 2026-05-26 - A successful `ui.bundle.refresh` updates the phone bundle on disk, but the active cover WebView may keep rendering the old UI until `CoverHomeActivity` is relaunched; verify both `ui.bundle.status` and a fresh `ui.surface.get loaded_at` or a post-restart screenshot.
- 2026-05-27 - `prove-displayable-reply-files --skip-refresh` can only validate the local UI bundle; if the VM-side attachment or icon endpoints have changed, live prompt proof must wait until the server code is pushed and the VM is updated.
- 2026-05-27 - For wake-turn emulator proof, assert progression from `pucky.turn.history` instead of a single live indicator snapshot; the blue/red/yellow states can advance faster than harness polling.
- 2026-05-27 - The emulator `flashlight` recipe path needs `android.permission.CAMERA`; without that grant, `wake-handoff-local` fails for environment reasons rather than wake routing.
- 2026-05-27 - The Links cover list should render page 1 immediately and fetch more only on near-bottom scroll or 2+ character search; eager 973-row hydration made cold opens and route restores feel stuck on the cover WebView.
- 2026-05-27 - For local Links browser probes, a `file:///.../pucky_vm/ui_src/index.html?route=links` headless Edge session can exercise the live Links API without taking over the user's browser, but use `window.__PUCKY_LINKS_DEBUG__` events instead of `performance` entries to tell whether stale browse requests were started before or after a search change.
- 2026-05-27 - The Links keyboard-dismiss bug on the cover WebView came from rebuilding the whole Links page during async updates; keep `#linksSearch` mounted and patch the list/connected chrome in place instead of replacing the route DOM.
- 2026-05-28 - If an emulator broker device disappears after repeated debug APK reinstalls, check `adb shell dumpsys activity activities` for `Application Not Responding` focus before blaming Pucky; a wedged system or permission-controller ANR can block broker reconnect even while the cover app process exists.
- 2026-05-28 - Links virtualization must clamp its start index and reset the feed scroll on search changes; otherwise a deep scroll plus a narrow local filter can render zero rows even though matching apps exist.
- 2026-05-27 - Scratch `prove-displayable-reply-files --skip-refresh` must install a local bundle after APK reinstall when `ui.bundle.status.ui_version` is blank, or the emulator stays on the fallback asset page and visual tile proof is invalid.
- 2026-05-27 - `api/feed` is cursor-paginated oldest-first in this stack, so proof harnesses must follow `next_cursor` to find recent turns instead of assuming the first page contains the newest card.
- 2026-05-28 - On the Razr cover, `adb shell screencap -d <display_id>` needs the secondary `SurfaceFlinger` display (HWC display 3 here); the primary display capture can come back as a black frame even while the cover UI is healthy.
- 2026-05-28 - `ui.reply_cards.set` is a destructive replace of the local reply-card store on Android; real-phone proof injection should use merge semantics or a feed sync reset, not set, or older home-feed cards will disappear.
- 2026-05-28 - `pucky.feed.sync` with `reset_cursor=true` is the restore path when the phone's local reply-card store was clobbered and `last_cursor` has advanced; it backfilled the older VM-backed feed without removing old failed outbound placeholders.
- 2026-05-28 - Real-device multi-live turn proof can run deterministically on the debug APK by using `pucky.turn.start` with `capture_source=fixture` plus `pucky.turn.debug.response_fault`; the emulator-generated `wake_weather.wav` and `wake_flashlight.wav` fixtures pass VAD reliably, while the small UI demo `.wav` files can be discarded as silence.
- 2026-05-28 - For shadow emulator broker proof, point `adb reverse tcp:18081` at a broker port you own and verify `hello.apk_identity.git_branch`; a stale shared fake broker can make a fresh debug install look unchanged even when the APK on disk is correct.
- 2026-05-28 - On the Android 15 emulator, `adb emu sms send` can return `OK` without creating `content://sms` provider rows, while `adb emu gsm call/cancel` does create missed-call rows in `content://call_log/calls`; treat SMS provider readback there as environment-limited proof, not an APK failure.
- 2026-05-28 - Scratch broker waits for `phone.sms.send`, `phone.calls.place`, and `phone.calls.hangup` need more than 15 seconds on the emulator; short waits can mislabel real completions as timeouts even when the broker later receives `completed` results.
- 2026-05-28 - On the live Razr, `phone.sms.send` and `phone.calls.place` both worked and read back on-device even while `phone.telephony.status` still reported the SMS and call surfaces as `role_needed`; treat that readiness as conservative diagnostics, not a hard execution gate.
- 2026-05-28 - On the live Razr, `android.permission.READ_BLOCKED_NUMBERS` and `WRITE_BLOCKED_NUMBERS` were not changeable with `pm grant`, and `com.android.voicemail.permission.READ_VOICEMAIL` was role-managed; blocked-number and voicemail commands should fail honestly unless the app truly acquires the needed role-managed access.
- 2026-05-28 - On the live Razr, normal Google Messages replies from `(407) 496-9882` reached `android.messages` notification extras and `dumpsys notification --noredact` even when `content://sms` never produced inbound rows; keep `phone.sms.get_thread` merged with notification-listener rows if you want plain-language reply readback to work against the real phone.
- 2026-05-29 - On the live Razr, ContactsContract can silently discard bad contact-photo bytes while `applyBatch` still returns; `android.contacts.photo.put` must verify a photo row/readback before reporting success.
- 2026-05-29 - On the live Razr, `content://user_dictionary/words` can query successfully but return a null URI on insert despite granted dictionary permissions; treat `android.user_dictionary.add` as unavailable unless insert returns a real URI.
- 2026-05-28 - When moving partial Windows work from a dirty canonical repo into an isolated worktree, generate diffs against worktree-relative paths; `git diff --no-index` patches with absolute `b/C:\...` paths will not reapply cleanly with `git apply`.
- 2026-05-27 - For real cover-phone interaction proof, use a forwarded `webview_devtools_remote_<pid>` socket plus DOM selectors; `uiautomator dump` only sees the outer `WebView` and is not trustworthy for tile or detail-surface clicks inside the cover shell.
- 2026-05-27 - For Walkie thread-continuation certification on a real phone, compare `status.get.apk_identity` and `ui.bundle.status` to canonical `master` before scenario testing; APK or bundle drift can look exactly like a routing failure.
- 2026-05-28 - For emulator-only Walkie continuation proof, prefer native `ui.debug.*` commands plus enriched `ui.surface.get` over WebView CDP; the harness can drive transcript/page/attachment focus and verify visible tile order without any device-only inspection lane.
- 2026-05-29 - Full APK command sweeps over the Fly broker can hit transient `DEVICE_OFFLINE` windows during long runs; certification harnesses should poll `devices` and wait through reconnects before marking endpoint rows failed.
- 2026-05-29 - The final-boss Walkie overlap proof can miss the intentionally short `thread_b` pending window if `ui.reply_cards` polling sleeps 2 seconds; use faster snapshot polling for overlap certification so the 1000 ms pending state is observable.
- 2026-05-29 - During emulator broker reconnects, `puckyctl ping` can succeed before the broker `devices` listing includes the emulator again; treat a successful ping against the target broker as command-channel proof instead of failing only on a stale device list.
- 2026-05-29 - Walkie lab reset needs a `pucky.feed.sync` cursor reset after clearing local reply cards; otherwise an advanced feed cursor can make VM-backed cards look missing even when the broker has them.
- 2026-05-29 - WebView detail panes can open even when `uiautomator` misses the expected title text; for emulator displayable-file proof, accept matching `ui.surface.get.detail` state as the authoritative open-detail signal.
- 2026-05-29 - Synthetic or replay-materialized reply cards are local APK state, not VM feed rows; archive proof for those cards must mutate `ui.reply_cards` locally instead of calling `pucky.feed.action`.
- 2026-05-29 - Phone Walkie continuation proof can miss the brief "Sending your message..." placeholder; keep polling the same thread tile and accept transcript-preview evidence when the pending card outlives the placeholder text.
- 2026-05-29 - On the live Fly Pucky VM, `gpt-5.3-codex-spark` at `PUCKY_CODEX_REASONING_EFFORT=medium` can hit the Codex usage cap while `low` still completes; if real-phone Walkie proof starts failing after `codex_running`, verify the live app-server budget before blaming thread routing.
- 2026-05-29 - Pucky VM defaults to Codex Spark with low reasoning effort for fast control-plane turns; override `PUCKY_CODEX_REASONING_EFFORT` only when a flow explicitly needs deeper reasoning.
- 2026-05-30 - Displayable proof replay cards can expose `Open page for ...` in the VM-built UI even when older attachment metadata also looks file-like; match the UI's page/file action label instead of deriving only from attachment snapshots.
- 2026-05-30 - Final-boss Walkie overlap proof needs long deterministic reply-delay floors because navigation and fixture capture can consume tens of seconds before the last turn starts; also fail the harness when any `proof.passes` bit is false.
- 2026-05-30 - Real-phone Walkie continuation proof can false-fail when it reuses older thread-backed cards whose upstream Codex threads are no longer resumable; seed fresh VM-backed proof cards or pass explicit fresh thread ids for final USB certification.
- 2026-05-30 - For live USB Walkie proof, non-final-boss turns may complete before the pending tile is observable; use `proof_reply_delay_ms` via the phone proof harness delay flags to hold a real pending window open long enough for placeholder and slot checks.
- 2026-05-30 - Home-feed tray icons that are derived only from live cards can make stable destinations like audiobooks disappear after feed churn; seed persistent home-menu icon entries for durable shortcuts such as the `book` tile.
- 2026-05-30 - On the repo-backed Fly VM, leave `PUCKY_UI_VERSION` unset; pinning it in the service environment can leave `ui_version` stale even while `source_commit_full` tracks the current checkout.
- 2026-05-30 - Fresh scratch or worktree emulator slots may not have an AVD yet; `pucky_emulator_suite.py start` should create the slot AVD on demand and fall back to `MainActivity` when the default cover launcher cannot be resolved during provisioning.
